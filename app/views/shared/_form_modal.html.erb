<%= turbo_frame_tag :remote_modal, target: :_top do %>
  <%# Added "data-bs-focus="false"" so slim slect can work inside a modal%>
  <div class="modal fade" data-controller="modal" tabindex="-1" data-bs-focus="false" >
    <div class="modal-dialog <%= defined?(size).nil? ? '' : size %>">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title"><%= title %></h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <%= render partial: partial, locals: { url: url } %>
        </div>
      </div>
    </div>
  </div>
<% end %>